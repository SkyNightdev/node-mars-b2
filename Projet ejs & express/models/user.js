const db = require('../util/database');

module.exports = class user {
    static adduser(userId) {
        
    }

    static removeuser(productId) {
        
    }

    static fetchAll() {
        
    }
}